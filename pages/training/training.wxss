/* ===========================
   🏋️ 训练页面 - Glassmorphism 风格
   =========================== */

.container {
  min-height: 100vh;
  padding: 40rpx 30rpx;
  position: relative;
  overflow: hidden;
  /* 使用统一的淡雅渐变背景 */
  background: linear-gradient(180deg, var(--bg-grad-start), var(--bg-grad-end));
}

.container::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: transparent;
  z-index: -1;
}

/* 页面标题 */
.header {
  margin-bottom: 40rpx;
  text-align: center;
}

/* 🎯 页面标题 - Glassmorphism 风格 */
.title {
  font-size: 56rpx;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 20rpx;
  letter-spacing: 2rpx;
  font-family: 'PingFang SC', 'Helvetica Neue', 'Microsoft YaHei UI', sans-serif;
}

.subtitle {
  font-size: 32rpx;
  color: var(--text-secondary);
  line-height: 1.5;
  font-weight: 500;
  letter-spacing: 1rpx;
}

/* 阶段选择视图 */
.stage-selection {
  margin-bottom: 40rpx;
}

.stage-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

/* 🎪 阶段卡片 - Glassmorphism 升级 */
.stage-card {
  background: var(--card-bg);
  border: 1rpx solid rgba(229, 231, 235, 0.5);
  border-radius: var(--radius-large);
  padding: 40rpx;
  display: flex;
  align-items: center;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  cursor: pointer;
  box-shadow: var(--shadow-light);
}

.stage-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3rpx;
  background: var(--gradient-primary);
  border-radius: var(--radius-large) var(--radius-large) 0 0;
}

.stage-card:active {
  transform: translateY(-2rpx) scale(0.98);
  box-shadow: var(--shadow-medium);
}

.stage-icon {
  font-size: 72rpx;
  margin-right: 36rpx;
}

.stage-info {
  flex: 1;
}

/* 🏷️ 阶段信息文字 */
.stage-name {
  font-size: 38rpx;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 12rpx;
  letter-spacing: 1rpx;
  font-family: 'PingFang SC', 'Helvetica Neue', 'Microsoft YaHei UI', sans-serif;
}

.stage-desc {
  font-size: 30rpx;
  color: var(--text-secondary);
  margin-bottom: 10rpx;
  line-height: 1.4;
  font-weight: 500;
}

.stage-count {
  font-size: 26rpx;
  color: var(--text-primary);
  background: var(--section-bg);
  padding: 10rpx 18rpx;
  border-radius: var(--radius-medium);
  display: inline-block;
  border: 1rpx solid rgba(229, 231, 235, 0.5);
  font-weight: 600;
}

.stage-arrow {
  font-size: 36rpx;
  color: var(--text-light);
}

/* 年级选择视图 */
.grade-selection {
  margin-bottom: 40rpx;
}

.back-header {
  margin-bottom: 36rpx;
}

/* 🔙 返回按钮 - 现代化设计 */
.back-btn {
  background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
  color: #5a6c7d;
  border: none;
  padding: 22rpx 36rpx;
  border-radius: 12rpx;
  font-size: 30rpx;
  font-weight: 600;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 4rpx 15rpx rgba(168, 237, 234, 0.3), 0 2rpx 8rpx rgba(254, 214, 227, 0.2);
  letter-spacing: 0.5rpx;
}



.back-btn:active {
  transform: translateY(2rpx) scale(0.98);
  box-shadow: 0 2rpx 10rpx rgba(168, 237, 234, 0.4), 0 1rpx 5rpx rgba(254, 214, 227, 0.25);
}

/* 📋 当前阶段信息卡片 */
.current-stage-info {
  background: var(--card-bg);
  border: 1rpx solid rgba(229, 231, 235, 0.5);
  border-radius: var(--radius-large);
  padding: 32rpx;
  margin-bottom: 36rpx;
  text-align: center;
  position: relative;
  overflow: hidden;
  box-shadow: var(--shadow-light);
}

.current-stage-info::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3rpx;
  background: var(--gradient-primary);
  border-radius: var(--radius-large) var(--radius-large) 0 0;
}

.stage-badge {
  margin-bottom: 18rpx;
}

.stage-icon-small {
  font-size: 48rpx;
  margin-right: 18rpx;
}

.stage-name-small {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.stage-desc-small {
  font-size: 28rpx;
  color: var(--text-secondary);
}

/* 年级列表 */
.grade-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

/* 🎓 年级卡片 - Glassmorphism 升级 */
.grade-card {
  background: var(--card-bg);
  border: 1rpx solid rgba(229, 231, 235, 0.5);
  border-radius: var(--radius-large);
  padding: 40rpx;
  box-shadow: var(--shadow-light);
  display: flex;
  justify-content: space-between;
  align-items: center;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

.grade-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3rpx;
  background: var(--gradient-primary);
  border-radius: var(--radius-large) var(--radius-large) 0 0;
}

.grade-card:active {
  transform: translateY(-2rpx) scale(0.98);
  box-shadow: var(--shadow-medium);
}

.grade-info {
  flex: 1;
}

.grade-name {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 18rpx;
}

.grade-stats {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.word-count {
  font-size: 28rpx;
  color: var(--text-secondary);
}

.progress-text {
  font-size: 28rpx;
  color: var(--secondary-color);
  font-weight: 600;
}

.learning-progress {
  font-size: 26rpx;
  color: var(--primary-color);
  font-weight: 500;
}

.detailed-progress {
  display: flex;
  gap: 12rpx;
  margin-top: 8rpx;
  font-size: 22rpx;
}

.mastered-text {
  color: var(--primary-color);
  font-weight: 600;
}

.error-text {
  color: var(--accent-color);
  font-weight: 600;
}

/* 📊 简化版年级统计样式 */
.grade-stats-simple {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.grade-stats-simple .word-count {
  font-size: 28rpx;
  color: var(--text-secondary);
  font-weight: 500;
}

.grade-stats-simple .mastered-text {
  font-size: 26rpx;
  color: var(--primary-color);
  font-weight: 600;
}

.unlearned-text {
  color: var(--text-light);
  font-weight: 500;
}

.grade-actions {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

/* 🚀 训练按钮 - 现代化设计 */
.training-btn {
  background: linear-gradient(135deg, #b3e5ff 0%, #89f7fe 10%);
  color: #0c0b0b;
  border: none;
  border-radius: 16rpx;
  padding: 24rpx 40rpx;
  margin-bottom: 10px;
  font-size: 32rpx;
  font-weight: 600;
  text-align: center;
  min-width: 160rpx;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  box-shadow: 0 8rpx 25rpx rgba(102, 126, 234, 0.25), 0 4rpx 12rpx rgba(118, 75, 162, 0.15);
  letter-spacing: 0.5rpx;
}





.training-btn:active {
  transform: translateY(2rpx) scale(0.98);
  box-shadow: 0 4rpx 15rpx rgba(102, 126, 234, 0.3), 0 2rpx 8rpx rgba(118, 75, 162, 0.2);
}

.training-btn.loading {
  opacity: 0.7;
  pointer-events: none;
}

/* 📊 进度按钮 - 现代化设计 */
.progress-btn {
  background: linear-gradient(135deg, #d6f6e6 0%, #b9efd6 10%);
  color: #181616;
  border: none;
  border-radius: 14rpx;
  padding: 20rpx 36rpx;
  margin-bottom: 10px;
  font-size: 30rpx;
  font-weight: 600;
  text-align: center;
  min-width: 160rpx;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 6rpx 20rpx rgba(240, 147, 251, 0.3), 0 3rpx 10rpx rgba(245, 87, 108, 0.2);
  letter-spacing: 0.5rpx;
}

.progress-btn:active {
  transform: translateY(2rpx) scale(0.98);
  box-shadow: 0 3rpx 12rpx rgba(240, 147, 251, 0.4), 0 2rpx 6rpx rgba(245, 87, 108, 0.25);
}

.progress-btn.loading {
  opacity: 0.7;
  pointer-events: none;
}

/* 底部提示 */
.footer-tip {
  margin-top: 40rpx;
  text-align: center;
}

/* 💡 底部提示 - Glassmorphism 风格 */
.tip-text {
  background: var(--section-bg);
  color: var(--text-secondary);
  font-size: 28rpx;
  padding: 24rpx 28rpx;
  border-radius: var(--radius-medium);
  line-height: 1.5;
  border: 1rpx solid rgba(229, 231, 235, 0.5);
  font-weight: 500;
  box-shadow: var(--shadow-light);
}

